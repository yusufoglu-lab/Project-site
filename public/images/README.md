# Images

Drop these here:

- `profile.jpg` — main portrait used on `/`, `/about`, `/team`
- `team/<first-name>-<last-name>.jpg` — team member portraits referenced from `data/team.ts`

The site falls back to monogram placeholders when an image is missing, so you can leave this directory empty during development.
